/* eslint-disable react/prop-types */

function StatelessComponent({ title, firstName, age }) {
  return (
    <div>StatelessComponent: {title} {firstName} {age}</div>
  )
}

export default StatelessComponent